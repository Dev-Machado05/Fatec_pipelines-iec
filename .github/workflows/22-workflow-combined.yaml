name: Workflow run combined Template
run-name: Running - Workdflow run combined Template
on: 
    workflow_run: 
        workflows: [Trigger]
        types: [requested, completed]

jobs: 
    quando_Trigger_for_disparado: 
        runs-on: ubuntu-latest
        steps: 
          - name: Comentário
            run: echo "workflow run combined foi trigado." 